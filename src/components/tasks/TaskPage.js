import React from 'react'

const TaskPage = () => {
    return (
        <div style={{display: 'flex', justifyContent: 'center' , alignItems: 'center'}}>
            Here will be placed task page connected to mongoDB server
        </div>
    )
}

export default TaskPage
